# ============================================================
#  AGV_Project .gitignore
#  原则：凡是「只对某台机器有效」的东西都不进仓库。
# ============================================================

# ---- 构建产物（随时可由 CMakeLists 重新生成）----
build/
mx.scratch

# ---- 本地缓存 / 索引（每个工具自己会重建）----
.codegraph/
.omo/
.tools/

# ---- 机器私有配置 ----
# 这些文件里存的是本机绝对路径（工具链位置等），提交上去就等着换机器时炸。
.claude/settings.local.json
.claude/*.local.json
.vscode/*.local.json
*.bak

# ---- 编辑器 / 系统噪声 ----
Thumbs.db
Desktop.ini
.DS_Store

# ============================================================
#  以下内容是「故意要提交」的，不要加进忽略：
#    .settings/       STM32Cube 工程元信息（器件型号、内核、toolchain、
#                     已装 bundle 版本）—— 相当于工具链的版本锁，
#                     换机器/换队友时靠它对齐版本
#    .vscode/*.json   共享的调试与任务配置（已全部改用 ${env:...} 变量）
#    .vscode/flash-retry.sh
#    .clangd / CMakePresets.json / CMakeLists.txt
# ============================================================
